# 邓智文 · Python 学习档案

> 更新日期：2026-09-07（周一）
> 说明：这个文件是给"下一次会话/换设备"看的接续文档。换电脑/换会话时，让 AI 读这个文件即可无缝衔接。
> Git 仓库：`github.com/cndengzw-create/day-learn`（git 连 GitHub 需走代理 `http://127.0.0.1:7897`，用户常挂梯子）
> 存档规则：每晚学完把当天代码另存一份到仓库根目录，并更新本档案。

---

## 一、我是谁 / 为什么学

- 银行 Java 开发者，约 6 年经验，技术栈较老（Spring 那套），自感技术停在 2021 年前
- 想重新提升：AI 时代很多东西与 Python 相关，不想继续落后
- 平时炒 **A 股**（上班用同花顺看盘），希望学完 Python 基础后做**股票相关小项目**练手（计划用 akshare 拉 A 股数据）
- 学习偏好：**文本 + 动手敲代码**，不看长视频教程
- 时间：每晚约 1 小时；白天摸鱼可再看一会儿（在公司外网笔记本上，那边没装 Python 环境，只能看不能敲）

## 二、环境

| 项目 | 状态 |
|---|---|
| Python | 3.13 ✅ 已装（`py --version` 可验证） |
| PyCharm | 2025.3 ✅（装的 community 版；显示"Pro 试用30天"是 JetBrains 统一产品正常现象，到期自动回落免费版，不用换） |
| 练习项目 | `D:\project\python\learn-first`（venv；代码文件按天建：day1.py、day2.py…） |
| 归档仓库 | `D:\project\python\day-learn`（Git 仓库，对应 GitHub 的 cndengzw-create/day-learn） |

⚠️ 注意：系统 PATH 里微软商店"假 python"占位符挡路，命令行请统一用 `py` 代替 `python`。

## 三、已完成进度

### 9/6 周日（热身 · 已完成）
1. 装好 Python 3.13 + PyCharm 2025.3
2. 建项目 `learn-first`，跑通第一个程序（见仓库 `day1_first_program.py`）：
   `f-string` 插变量、`range(10)`、列表推导式 `[x*2 for x in range(10) if x%2==0]`
3. 概念已懂：f-string、range、列表推导式（= Java 的 for+if+add 压缩版）、解释器 vs IDE、Shift+F10 运行 / Alt+4 开输出窗口

### 9/7 周一（第一课 · 变量/字符串/数字 · 已完成）
- **动态类型 vs Java 静态类型**：变量是"标签"不是"盒子"；`years_java = 6` 后可改成 `"六年"` 不报错；类型跟着值走
- `type()` 查类型；基础类型 str / int / float / bool；**Python 没有 char**（单双引号都是 str）
- 运算符全家：`/` 真除（7/2=3.5）、`//` 向下取整、`%` 取余、`**` 幂
  - **`//` 与 Java 不同**：向下（往负无穷），`-7//2 = -4`；Java 是向零截断得 -3
  - **`%` 余数符号跟随除数**：`-7%2 = 1`（除数正→余数恒非负）；`5%-3 = -1`
  - `int()` 朝零截断（`int(-3.5)=-3`）vs `//` 朝下（`-3.5//1=-4`）——两个"取整"方向不同
- 字符串：单双引号互相包住免转义（`'他说："…"'`）、`\n \t \\` 转义、三引号跨行文本、`+` 拼接、`*` 重复（`"哈"*3`）
- **类型转换必须显式**：`str()/int()/float()`；`"工龄" + 6` 报 `TypeError`（Python 不像 Java 自动转）
  - `int("18.5")` 报 `ValueError`——int 只认整数字符串，带小数点要用 float()
- **f-string**：f = format；字符串里想插 `{变量}` 才需要 f，不加 f 则 `{}` 原样打印
- `input()` 交互：程序停下等用户输入，**返回永远是字符串**，要计算先 int()/float() 转
- 踩坑实录：手滑把 name 敲成 neme → PyCharm 自动导入 `from os import name` 顶掉了自定义 name（打印出 `nt`）。教训：拼写错会触发自动导入，警惕代码顶部陌生的 `from ... import`；变量名别和模块属性撞车
- 今日代码：`learn-first/day1.py`（全部练习）；仓库存档 `day2_variables_strings_numbers.py`
- 今日产出小结：学会后做了一个"自我介绍问答机"小练习（用了 input + if，if 明天正式学）

## 四、明日任务（9/8 周二 · 第二课 集合）

- 主题：**list（列表）/ tuple（元组）/ dict（字典）/ set（集合）**（对照 Java 的 List/数组/Map/Set）
- 教材：廖雪峰 Python 教程 → **第 5 章 "Python 基础"**（后半部分：list、tuple、dict、set）
- 产出：在 `learn-first` 建 `day2.py` 练习，学完存仓库一份
- 备注：可以提前预热"切片"和"列表推导式"（昨天已见过推导式雏形）

## 五、我的学习计划总览（16 周）

- 第 1-2 周：环境 + 热身（已完成）
- 第 3-8 周：语法主干（对比学习：list/dict/set、函数、模块、类……）
- 第 9-16 周：职场自动化实战（openpyxl/pandas 处理报表、requests 爬接口）+ 股票小项目起步
- 教材只学核心区（第 5~13 章挑着学），Web/异步等暂时跳过
- 参考书：《Python 编程快速上手——让繁琐工作自动化》

## 六、与 AI 助手配合的注意事项

- 边界感：只问问题时，**不要未经确认就擅自安装/下载/执行**——先问再做
- 记忆：AI 有长期学习档案（跨会话），如读不到，让它读本文件即可
- git 访问 GitHub：自动带代理 `-c http.proxy=http://127.0.0.1:7897`（用户常挂梯子，梯子没开时 push 会失败，提醒用户开梯子即可）
