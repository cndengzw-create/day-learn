# 邓智文 · Python 学习档案

> 更新日期：2026-09-06（周日）
> 说明：这个文件是给"明天的我"看的接续文档。换电脑/换会话时，让 AI 读这个文件即可无缝衔接。

---

## 一、我是谁 / 为什么学

- 银行 Java 开发者，约 6 年经验，技术栈较老（Spring 那套），自感技术停在 2021 年前
- 想重新提升：AI 时代很多东西与 Python 相关，不想继续落后
- 平时炒 **A 股**（上班用同花顺看盘），希望学完 Python 基础后做**股票相关小项目**练手（计划用 akshare 拉 A 股数据）
- 学习偏好：**文本 + 动手敲代码**，不看长视频教程
- 时间：每晚约 1 小时；白天摸鱼可再看一会儿（在公司外网笔记本上）

## 二、环境（家中电脑）

| 项目 | 状态 |
|---|---|
| Python | 3.13 ✅ 已装（`py --version` 可验证） |
| PyCharm | 2025.3 ✅（装的 community 版；显示"Pro 试用30天"是 JetBrains 统一产品正常现象，到期自动回落免费版，不用换） |
| 练习项目 | `D:\project\python\learn-first`（venv，解释器已关联 Python 3.13） |
| 首个程序 | `main.py` 已跑通 ✅ |

⚠️ 注意：系统 PATH 里微软商店"假 python"占位符挡路，命令行请统一用 `py` 代替 `python`。

## 三、今日进度（9/6 周日）

1. 装好 Python 3.13 + PyCharm 2025.3
2. 在 PyCharm 建项目 `learn-first`，跑通第一个程序：

```python
name = "邓智文"
years = 6
print(f"我叫{name}，写Java已{years}年")   # f-string：字符串里用 {} 插变量
nums = [x * 2 for x in range(10) if x % 2 == 0]
print(nums)                              # 列表推导式 → [0, 4, 8, 12, 16]
```

3. 已懂的概念（与 Java 对照）：
   - **f-string**：`f"..."` 里 `{}` 直接插变量/表达式（类似 Java 字符串拼接，但更爽）
   - **range(10)**：生成 0~9（相当于 Java `for(int x=0; x<10; x++)` 的计数部分）
   - **列表推导式**：`[对x做啥 for x in 数据 if 条件]` = Java 的 `for` + `if` + `list.add()` 三件套压缩版
   - **解释器 vs IDE**：Python 有多种解释器（CPython 官方 / PyPy / Jython / IronPython），CPython 最主流；PyCharm 是 **IDE**（壳），自己不执行代码，点运行按钮时调用 CPython。类比：PyCharm 之于 CPython = IDEA 之于 JDK
   - 廖雪峰建议：Python 与 Java/.NET 交互，别用 Jython/IronPython，用网络调用（相当于跨语言微服务思想）
4. 常用快捷键：**Shift+F10** 运行程序；**Alt+4** 打开/收起底部运行输出窗口

## 四、明日任务（9/7 周一）——第一次正式上课

- 主题：**变量、字符串、数字**（对照 Java 强类型 vs Python 动态类型）
- 教材：廖雪峰 Python 教程 → **第 5 章 "Python 基础"**（前半部分：整数/浮点/字符串/布尔/空值 + 变量）
- 产出：写几行代码，用 `print` 输出名字 + 工龄（可以再加点自我介绍）
- 节奏：40 分钟学 + 20 分钟动手敲，学完在项目里建 `day1.py` 存起来

## 五、我的学习计划总览（16 周）

- 第 1-2 周：环境 + 热身（已完成）
- 第 3-8 周：语法主干（对比学习：list/dict/set、函数、模块、类……）
- 第 9-16 周：职场自动化实战（openpyxl/pandas 处理报表、requests 爬接口）+ 股票小项目起步
- 教材只学核心区（第 5~13 章挑着学），Web/异步等暂时跳过
- 参考书：《Python 编程快速上手——让繁琐工作自动化》

## 六、与 AI 助手配合的注意事项

- 边界感：只问问题时，**不要未经确认就擅自安装/下载/执行**——先问再做
- 记忆：AI 有长期学习档案（跨会话），如读不到，让它读本文件即可
